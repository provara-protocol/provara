# Provara Protocol v1.0.2 — Social Copy

## X (Short)
Provara Protocol v1.0.2 is live on PyPI.

Patch release focused on package reliability:
- `provara verify` and `provara backup` now work in normal pip installs
- Removed test-only runtime dependency path
- Added internal integrity checks + fixed verify side-effect state mutation

PyPI: https://pypi.org/project/provara-protocol/1.0.2/
Release: https://github.com/provara-protocol/provara/releases/tag/v1.0.2

## X (Thread Starter)
Shipped: Provara Protocol v1.0.2.

This is a reliability patch for real-world installs. `verify`/`backup` no longer rely on test-only modules, and verification paths are now read-only where expected.

If you pulled 1.0.1, upgrade:
`pip install -U provara-protocol==1.0.2`

## LinkedIn
Provara Protocol v1.0.2 is now live.

This patch release fixes packaging/runtime behavior discovered during post-release smoke validation:

- `provara verify` and `provara backup` no longer depend on test-only modules in pip environments
- Added package-internal integrity checks (manifest content, Merkle root, causal chains, event signatures)
- Removed verification side effects that could create mutable sidecar state during read-only checks

Install / upgrade:
`pip install -U provara-protocol==1.0.2`

PyPI: https://pypi.org/project/provara-protocol/1.0.2/
GitHub release notes: https://github.com/provara-protocol/provara/releases/tag/v1.0.2

If you’re building AI systems that need tamper-evident memory and verifiable event history, feedback is welcome.
